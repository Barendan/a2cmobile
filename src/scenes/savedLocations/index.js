import React from 'react';
import { SafeAreaView, Text } from 'react-native';
import { Inset } from "react-native-spacing-system";

const SavedLocations = () => (
  <SafeAreaView>
    <Inset all={16}>
      <Text>Screen: Saved Locations</Text>
    </Inset>
  </SafeAreaView>
);

export default SavedLocations;