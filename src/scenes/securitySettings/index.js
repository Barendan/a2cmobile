import React from 'react';
import { SafeAreaView, Text } from 'react-native';
import { Inset } from "react-native-spacing-system";

const SecuritySettings = () => (
  <SafeAreaView>
    <Inset all={16}>
      <Text>Screen: Security Settings</Text>
    </Inset>
  </SafeAreaView>
);

export default SecuritySettings;