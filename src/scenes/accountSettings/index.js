import React from 'react';
import { SafeAreaView, Text } from 'react-native';
import { Inset } from "react-native-spacing-system";

const AccountSettings = () => (
  <SafeAreaView>
    <Inset all={16}>
      <Text>Screen: Account Settings</Text>
    </Inset>
  </SafeAreaView>
);

export default AccountSettings;